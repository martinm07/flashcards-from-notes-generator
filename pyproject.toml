[project]
name = "past-paper-notes-flashcard-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown>=3.10.2",
    "requests>=2.33.1",
]

[project.scripts]
convert-exam-notes = "convert_exam_notes.main:main"
