text = """
Paper 1 (March 2021)
---
- When finding the domain of composed functions, it is NOT the same as taking the intersection of the domains of the two functions being composed― for gf(x), the RANGE of f(x) makes the DOMAIN of g(x); the domain of gf(x) must be found through analysis, but will be a subset of the domain of f(x), specifically, the subset of values that make results within the domain of g(x).

- When finding the set of possible values of a variable, remember that if that variable is part of a geometric diagram, it (probably) CAN'T be negative
- When finding the set of possible values of a variable, read all parts of the previous question carefully for ALREADY PROVIDED upper or lower bounds. If one bound is given, just focus on finding the other.
- Note that when a range of an independent variable is stated, and you're looking for the set of possible values of a dependent variable, it may not necessarily be done through plugging in the extremes of that bound into an expression― see if that stated range has another purpose, like ensuring physically sensible values from a geometric diagram.

- It is unnecessary to rewrite line equations into the strict form y = mx + b. It can be left however it was found, typically (unless a specific form was explicitly asked).

- Remember to multiply out brackets :))
- Make sure to copy numbers from the question correctly! Not mistakenly writing -1 instead of 1 (perhaps especially when another value to copy is minus instead)
- Don't make squares appear out of nowhere :0

Set 2 Paper 4 (Nov 2022)
---
- Everything correct― no notes 🎉

Set 2 Paper 3 (Nov 2021)
---
- Remember that questions involving complex numbers and Argand diagrams may be more easily solved geometrically― i.e. inscribing (potentially multiple) triangles, especially right triangles where we only need to know two lengths to find an angle or another side, etc.

- Some trigonometric "show that" (involving triple equals) don't necessarily expect pure expression manipulation, and may expect equation manipulation instead, especially if an expression can be simplified/expanded in two different ways, both forms are equated as they must be equivalent.
- It's a good idea to explicitly state trigonometric identities, like double angle identities before using them (it may be expected by the MS in "show that" questions!)

- When finding values that satisfy a certain condition (for example where the gradient of a curve is 1), a solution is already known (e.g. already given), and a question asks for *ANOTHER* point/value(s) satisfying that condition, and you're to provide an equation for those solution(s), it should NOT have the known point as a solution― you may need to think about algebraic division of that root to remove it as a solution from the resulting quotient.

- Remember to simplify final answers given as logarithms (it may not be exactly necessary unless a specific form for the final answer is explicitly asked for, but good practice anyway)
- Don't randomly omit parts of an expression when simplifying it :D

Set 2 Paper 1 (June 2021)
---
- Prefer to use the term "stretch" rather than "scale" when describing transformations

- Note these questions that give a series of values approach a certain point, and the convergence of the gradient as the derivative at that point. Important terminology: "the GRADIENT at [point] is EQUAL TO the LIMIT of the *GRADIENTS of the CHORDS* as the X-VALUE tends to [value]"

- Make special note of what a perpendicular bisector tells you (given you have the equation describing one). It gives you the GRADIENT of the LINE between the two points, through -1/m. It is also known that the *intersection* of the bisector line and the line between the two points, will be the MIDPOINT of the two points. There are often easier ways to solve equations involving geometry than might first meet the eye.

- Make special note of "at [point]" in questions, like "l is tangent to the circle *at A*". It is small but tells you a lot! (like, perhaps the coordinates of the intersection).
- Remember, b^2 - 4ac is called the DISCRIMINANT.

- Remember the volume of revolution formula, as the continuous form of the sum of a bunch of disk volumes Σ πr² δx
In particular, it is NOT squaring the result of a definite integral.

- Just like, make sure you're reading the right numbers from the question? :)
- Evaluate substitutions correctly... Try using the brain more in preference over the calculator for a bit more common sense?
- Make sure not to get confused with quoted formulas using variable names already in use.

Set 1 Paper 1 (June 2021)
---
- Remember, the area of a "pizza slice" of a circle is a SEGMENT (and the area on one half of a chord is called a SECTOR).

- There are questions which require graphical inspection of a transformed curve to figure out the transformation applied (NOT algebra, but visual inspection). First find the translation, most helpfully using the origin and the new location of what should be the origin point. Then, figure out the scaling, by seeing the translation of another point to a point where the required scale can be figured out (something like the translated point that should be at (π/4, 1) is at (π/4, 2), so a scaling factor of 2 in the y-direction was used.
- Try playing out the proposed transformation forwards, to verify they make sense.

- The easier way of evaluating/expanding a set of composed functions is INSIDE OUT, simplifying at each step (mostly relevant for when more than two functions are being composed together).

- Just... make sure keywords have *legible* handwriting in English answers.

- But overall, this scored full marks 🎉

Set 3 Paper 4 (June 2021)
---
- When writing answers in mechanics papers, if an infinite decimal would be required and a simple fraction could be used instead, just use the fraction...

- When sketching a parabola (quadratic), there are (up to) 6 points that should be clearly labelled; the y-axis intercept, the two x-axis intersections, the min/max, and the two extreme points between which the parabola is defined (i.e. the leftmost and rightmost defined points).

- Given we know the overall acceleration of a set of connected objects, we can just use one of the objects to figure stuff out. We do NOT need to find the expressions for the accelerations of each and equate them.

- It can be important to utilise the fact that the peak hight of a rising/falling particle (whose motion follows a quadratic) is reached HALFWAY between any two points with the same y-level. This can be used to solve, for example, "the height of the particle above the ground is more than x metres for a period of 3.6 seconds― find x". After 3.6/2 = 1.8 seconds of falling from the peak height, we'll have the point with the y-level for which the other point with the same y-level is 3.6 seconds away.

- Take careful note of if the question asks for SPEED or VELOCITY. And note that speed DOESN'T HAVE A SIGN.

================
- MISCELLANEOUS
===============

- For trigonometry, it's important to (quickly) remember/recognise that cos θ tan θ ≡ sin θ
- For trigonometry, it's important to (quickly) remember/recognise the identity 1 + tan²θ ≡ sec²θ
- For integration of sin²x or cos²x, REMEMBER the double angle identity for cos (and use it in reverse!): cos 2x = cos²x - sin²x
...which means that cos²x = (cos 2x + 1) / 2 and sin²x = (1 - cos 2x) / 2

- Specific solutions of differential equations don't have ± (as such an equation represents TWO specific solutions). Make sure to resolve the ± into a + or a - based on the information available.
- When solving differential equations, we often omit the absolute sign that comes from integrating 1/x (i.e. in ln |x|), because we assume solutions to be differentiable functions, which isn't true for one point in y = |x| (the "kink" in the curve where x = 0). We resolve the |x| to + or - based on a known point in the solution, and seeing whether the evaluation at that point takes the value within the absolute function to positive or negative.

- Always be careful of frictional forces changing direction when an object changes directions! Most notably when an object is going up and is influenced by gravity, it eventually starts travelling down/falling (hence changing direction)!
- Remember the difference between resultant force and acceleration― connected objects will have the same acceleration but NOT the same resultant force being applied to each (overall).
"""

from parser import parse_notes
import textwrap

parsed = parse_notes(text, "Maths")

print("\n--------------\n")

for note in parsed:
    print(f"SOURCE: {note['source']} NOTE: {textwrap.shorten(note['content'], width=50, placeholder="…")}")
